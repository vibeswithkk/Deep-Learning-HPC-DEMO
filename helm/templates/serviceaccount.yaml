{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "deep-learning-hpc-demo.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "deep-learning-hpc-demo.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}